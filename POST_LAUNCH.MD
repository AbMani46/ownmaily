# Post-Launch Improvements

- Interactive install script or CLI that walks the user through setup interactively, replacing manual .env editing. Could be a shell script with prompts or a small Go binary.
